<button type="button" class="btn btn-danger lift" onclick="document.getElementById('delete-form').submit()">
    <i class="fas fa-trash me-1"></i> Supprimer
</button>

<form id="delete-form" method="post" action="{{ path('app_ressource_delete', {'id': ressource.id}) }}" class="d-none">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ressource.id) }}">
</form>