<form method="post" action="{{ path('app_reservation_session_delete', {'id': reservation_session.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_session.id) }}">
    <button class="btn">Delete</button>
</form>
