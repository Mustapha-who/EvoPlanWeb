<form method="post" action="{{ path('app_session_delete', {'id_session': session.id_session}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session.id_session) }}">
    <button class="btn">Delete</button>
</form>
