<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Rapport des Réclamations</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            color: #333;
        }
        h1 {
            color: #007bff;
            text-align: center;
            border-bottom: 2px solid #007bff;
            padding-bottom: 10px;
        }
        h2 {
            color: #444;
            margin-top: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #007bff;
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        .stats {
            margin-top: 20px;
            padding: 10px;
            border: 1px solid #ddd;
            background-color: #f9f9f9;
        }
        .filters {
            margin-top: 20px;
            padding: 10px;
            border: 1px solid #ddd;
            background-color: #f9f9f9;
        }
        .date {
            text-align: right;
            font-size: 10px;
            color: #777;
        }
    </style>
</head>
<body>
    <div class="date">
        Rapport généré le : {{ date|date('d/m/Y H:i:s') }}
    </div>

    <h1>Rapport des Réclamations</h1>

    <div class="filters">
        <h2>Filtres appliqués</h2>
        <p><strong>Statut :</strong> {{ filters.claimStatus|default('Aucun') }}</p>
        <p><strong>Type :</strong> {{ filters.claimType|default('Aucun') }}</p>
    </div>

    <div class="stats">
        <h2>Statistiques</h2>
        <p><strong>Nouvelles :</strong> {{ status_counts.new }}</p>
        <p><strong>En cours :</strong> {{ status_counts.in_progress }}</p>
        <p><strong>Résolues :</strong> {{ status_counts.resolved }}</p>
    </div>

    <h2>Liste des Réclamations</h2>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Description</th>
                <th>Type</th>
                <th>Statut</th>
                <th>Date</th>
                <th>Client (Numéro)</th>
            </tr>
        </thead>
        <tbody>
            {% for claim in claims %}
                <tr>
                    <td>{{ claim.id }}</td>
                    <td>{{ claim.description|slice(0, 100) }}{% if claim.description|length > 100 %}...{% endif %}</td>
                    <td>{{ claim.claimType }}</td>
                    <td>{{ claim.claimStatus }}</td>
                    <td>{{ claim.creationDate|date('d/m/Y') }}</td>
                    <td>{{ claim.client ? claim.client.phoneNumber : 'N/A' }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">Aucune réclamation trouvée.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>